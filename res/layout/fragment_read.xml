<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_height="fill_parent"
              android:layout_width="fill_parent"
              android:padding="12dp"
              android:orientation="vertical">

  <RelativeLayout android:layout_height="fill_parent"
                  android:layout_width="fill_parent"
                  android:padding="12dp">

    <TextView android:id="@+id/textBillboard"
              style="@style/default_text"
              android:layout_width="fill_parent"
              android:layout_height="wrap_content"
              android:layout_centerInParent="true"
              android:textSize="20dp"
              android:text="Please add page count"
              android:gravity="center"/>

    <ImageView android:id="@+id/tracker"
               android:src="@drawable/spinner_sketch"
               android:layout_centerInParent="true"
               android:layout_width="fill_parent"
               android:layout_height="fill_parent"
               android:contentDescription="spinner"
               android:scaleType="fitCenter"/>

  </RelativeLayout>

  <!-- flipper between the start and pause/done button -->
  <include layout="@layout/_reading_session_controls"/>

</LinearLayout>
