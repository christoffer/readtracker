<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.readtracker.android"
          android:versionCode="15"
          android:versionName="3.0.4">

  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

  <supports-screens
    android:largeScreens="true"
    android:normalScreens="true"
    android:smallScreens="false"/>

  <application
    android:allowBackup="true"
    android:icon="@drawable/icon"
    android:label="@string/app_name"
    android:name=".ReadTrackerApp"
    android:theme="@style/ReadTrackerTheme">

    <activity android:name=".activities.HomeActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity
      android:name=".activities.BookSearchActivity"
      android:label="@string/activity_label_add_book"
      android:parentActivityName=".activities.HomeActivity"
      android:launchMode="singleTop">
      <meta-data
        android:name="android.support.PARENT_ACTIVITY"
        android:value=".activities.HomeActivity"/>
    </activity>

    <activity
      android:name=".activities.AddBookActivity"
      android:noHistory="true"
      android:label="@string/activity_label_add_book"
      android:windowSoftInputMode="adjustPan"/>

    <activity
      android:name=".activities.BookActivity"
      android:parentActivityName=".activities.HomeActivity"
      android:label=""
      android:launchMode="singleTop">
      <!-- Parent activity meta-data to support 4.0 and lower -->
      <meta-data
        android:name="android.support.PARENT_ACTIVITY"
        android:value=".activities.HomeActivity"/>
    </activity>

    <activity
      android:name=".activities.FinishBookActivity"
      android:parentActivityName=".activities.BookActivity">
      <meta-data
        android:name="android.support.PARENT_ACTIVITY"
        android:value=".activities.BookActivity"/>
    </activity>

    <activity
      android:name=".activities.AddQuoteActivity"
      android:parentActivityName=".activities.BookActivity">
      <meta-data
        android:name="android.support.PARENT_ACTIVITY"
        android:value=".activities.BookActivity"/>
    </activity>

    <activity
      android:name=".activities.SettingsActivity"
      android:label="@string/activity_label_settings"
      android:parentActivityName=".activities.HomeActivity">
      <meta-data
        android:name="android.support.PARENT_ACTIVITY"
        android:value=".activities.HomeActivity"/>>
    </activity>

    <activity
      android:name=".activities.BookSettingsActivity"
      android:label="@string/activity_label_book_settings"
      android:parentActivityName=".activities.BookActivity">
      <meta-data
        android:name="android.support.PARENT_ACTIVITY"
        android:value=".activities.BookActivity"/>
    </activity>

    <activity android:name=".activities.InAppBrowserActivity"/>

  </application>

</manifest>
